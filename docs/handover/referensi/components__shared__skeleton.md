# `components/shared/skeleton.tsx`

| | |
|---|---|
| **Area** | Komponen Antarmuka |
| **Ukuran** | 117 baris |
| **Jenis** | modul biasa |
| **Dipakai oleh** | 0 berkas |

## Maksud berkas

Berkas ini menyediakan 5 fungsi utama: `Skeleton`, `CardSkeleton`, `TableRowSkeleton`, `FormSkeleton`, `TablePageSkeleton`.

## Letak berkas

Dari akar repo: `components/shared/skeleton.tsx`.

## Isi yang bisa dipakai berkas lain

### `Skeleton`

Jenis: **fungsi**

Skeleton component untuk loading states. Gunakan di loading.tsx atau sebagai fallback Suspense. Usage: <Skeleton className="h-4 w-24" /> /

### `CardSkeleton`

Jenis: **fungsi**

Card skeleton — cocok untuk stats cards di dashboard. /

### `TableRowSkeleton`

Jenis: **fungsi**

Table row skeleton — cocok untuk daftar baris tabel. /

### `FormSkeleton`

Jenis: **fungsi**

Form skeleton — untuk form modal/dialog. /

### `TablePageSkeleton`

Jenis: **fungsi**

Table page skeleton — komplit untuk halaman tabel. /

## Berkas lain di proyek ini yang dipanggil

- [`lib/utils/format.ts`](./lib__utils__format.md)

