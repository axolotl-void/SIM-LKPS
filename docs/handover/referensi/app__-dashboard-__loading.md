# `app/(dashboard)/loading.tsx`

| | |
|---|---|
| **Area** | Halaman & API (App Router) |
| **Ukuran** | 6 baris |
| **Jenis** | modul biasa |
| **Dipakai oleh** | 0 berkas |

## Maksud berkas

No loading screen for dashboard — content loads fast enough. Full LoadingScreen only shows inside bab-* table routes.

## Letak berkas

Dari akar repo: `app/(dashboard)/loading.tsx`.

## Isi yang bisa dipakai berkas lain

### `Loading`

Jenis: **default**

No loading screen for dashboard — content loads fast enough. Full LoadingScreen only shows inside bab-* table routes.

