# `app/api/auth/[...nextauth]/route.ts`

| | |
|---|---|
| **Area** | Halaman & API (App Router) |
| **Ukuran** | 4 baris |
| **Jenis** | modul biasa |
| **Alamat URL** | `/api/auth/[...nextauth]` |
| **Dipakai oleh** | 0 berkas |

## Maksud berkas

Berkas API untuk alamat `/api/auth/[...nextauth]`. Di sinilah permintaan dari browser diproses dan jawabannya disusun.

## Letak berkas

Dari akar repo: `app/api/auth/[...nextauth]/route.ts`.

## Alamat yang dilayani

Halaman ini bisa dibuka di `/api/auth/[...nextauth]`.

Kalau mau mengubah apa yang tampil di alamat itu, **berkas ini yang pertama dibuka**.

## Berkas lain di proyek ini yang dipanggil

- [`lib/auth.ts`](./lib__auth.md)

