# `app/api/health/route.ts`

| | |
|---|---|
| **Area** | Halaman & API (App Router) |
| **Ukuran** | 21 baris |
| **Jenis** | modul biasa |
| **Alamat URL** | `/api/health` |
| **Dipakai oleh** | 0 berkas |

## Maksud berkas

Berkas API untuk alamat `/api/health`. Di sinilah permintaan dari browser diproses dan jawabannya disusun.

## Letak berkas

Dari akar repo: `app/api/health/route.ts`.

## Isi yang bisa dipakai berkas lain

### `GET`

Jenis: **fungsi async**

## Alamat yang dilayani

Halaman ini bisa dibuka di `/api/health`.

Kalau mau mengubah apa yang tampil di alamat itu, **berkas ini yang pertama dibuka**.

## Pustaka luar yang dipakai

- `next/server`

## Berkas lain di proyek ini yang dipanggil

- [`lib/db.ts`](./lib__db.md)

