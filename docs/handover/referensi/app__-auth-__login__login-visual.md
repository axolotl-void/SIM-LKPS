# `app/(auth)/login/login-visual.tsx`

| | |
|---|---|
| **Area** | Halaman & API (App Router) |
| **Ukuran** | 123 baris |
| **Jenis** | modul biasa |
| **Dipakai oleh** | 1 berkas |

## Maksud berkas

Berkas ini menyediakan 1 fungsi utama: `LoginVisual`.

## Letak berkas

Dari akar repo: `app/(auth)/login/login-visual.tsx`.

## Isi yang bisa dipakai berkas lain

### `LoginVisual`

Jenis: **fungsi**

Left pane branding. Mirrors DESAIN-LOGIN-PAGE 1:1: UBBG lockup (gradient square + school icon + UBBG text) Center: SIMLKPS headline (gradient on "LKPS") + sub-heading w/ gradient bar + paragraph + image ilustrasi Footer "© 2026 BTIK" Visual stagger handled by CSS (login-stagger-N classes from globals.css). Pointer-events enabled inside the aside since shell sets pointer-events:none. /

## Pustaka luar yang dipakai

- `next/image`

## Berkas yang memanggil berkas ini

Kalau kamu mengubah nama fungsi atau bentuk datanya, berkas-berkas ini ikut terdampak:

- [`app/(auth)/login/page.tsx`](./app__-auth-__login__page.md)

